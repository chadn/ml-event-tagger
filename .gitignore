# Python
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
ENV/
env/
*.egg-info/
dist/
build/

# ML artifacts (training outputs - but keep trained model for deployment!)
# models/*.h5  <- COMMENTED OUT - we need this for deployment!
# models/tokenizer.json  <- COMMENTED OUT - we need this for deployment!
# models/model_config.json  <- COMMENTED OUT - we need this for deployment!
models/training_history.json  # Optional - not needed for inference
models/*.png  # Training plots - not needed for inference

# Data
data/events-raw-fb.json
data/*.npy
data/tag_names.json

# IDE
.vscode/
.idea/
.DS_Store

# Jupyter
.ipynb_checkpoints/
notebooks/*.html
notebooks/*.pdf
*.ipynb_checkpoints

# Logs
*.log

# Environment
.env
